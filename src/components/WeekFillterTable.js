import React from 'react';
import { useParams } from 'react-router-dom';

function WeekFilterTable() {
  const { subject, week } = useParams();

  // ნიშნები ვარენდეროთ უნდა აქ
}


export default WeekFilterTable;
